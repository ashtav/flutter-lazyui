import 'package:flutter/material.dart';

class LzImage extends StatelessWidget {
  final dynamic src;
  final dynamic size;
  const LzImage(this.src, {super.key, this.size});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
