import 'package:flutter/material.dart';

class SwitchStyle {
  final Color? textColor;
  final Color? activeColor;
  final Color? trackColor;
  final Color? thumbColor;

  SwitchStyle({this.textColor, this.activeColor, this.trackColor, this.thumbColor});
}
