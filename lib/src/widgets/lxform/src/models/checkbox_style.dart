import 'package:flutter/material.dart';

class CheckboxStyle {
  final Color? background, borderColor;
  final double? radius;
  final Color? textColor, activeColor, inactiveColor;

  CheckboxStyle({this.background, this.borderColor, this.radius, this.textColor, this.activeColor, this.inactiveColor});
}
