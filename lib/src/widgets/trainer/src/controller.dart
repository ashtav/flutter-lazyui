import 'package:flutter/material.dart';

/// Controller for managing the visibility of the trainer.
class TrainerController {
  /// Callback function to show the trainer.
  VoidCallback show = () {};

  /// Callback function to hide the trainer.
  VoidCallback hide = () {};
}
