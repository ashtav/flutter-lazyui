import 'package:flutter/material.dart';

class RadioStyle {
  final Color? background, borderColor;
  final double? radius;
  final Color? textColor, activeColor, inactiveColor;

  RadioStyle({this.background, this.borderColor, this.radius, this.textColor, this.activeColor, this.inactiveColor});
}
